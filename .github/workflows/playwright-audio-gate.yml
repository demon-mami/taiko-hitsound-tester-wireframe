name: Playwright Audio Gate

on:
  push:
    branches:
      - main
      - "integration/**"
  pull_request:

permissions:
  contents: write

jobs:
  practical-audio-gate:
    timeout-minutes: 25
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref || github.ref_name }}
      - uses: actions/setup-node@v6
        with:
          node-version: "22"

      - name: Apply Mobile timeline focus patch
        if: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/integration/v23-stage6-responsive-2hits') || (github.event_name == 'pull_request' && github.head_ref == 'integration/v23-stage6-responsive-2hits') }}
        run: |
          if grep -q 'const MOBILE_TIMELINE_X_SCALE = 1.15;' script.js; then
            echo "Mobile timeline focus patch already applied."
          else
            node tools/apply-mobile-timeline-focus.mjs
          fi

      - name: Install test dependency
        run: npm install

      - name: Install Chromium and WebKit
        run: npx playwright install --with-deps chromium webkit

      - name: Run practical audio gate
        run: npm run test:e2e
        env:
          CI: "true"

      - name: Commit Mobile timeline focus implementation
        if: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/integration/v23-stage6-responsive-2hits') || (github.event_name == 'pull_request' && github.head_ref == 'integration/v23-stage6-responsive-2hits') }}
        run: |
          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add script.js style.css tests/mobile-ui.spec.mjs README_STAGE6.md
          git commit -m "Focus mobile layout on Object Timeline"
          git pull --rebase origin integration/v23-stage6-responsive-2hits
          git push origin HEAD:integration/v23-stage6-responsive-2hits

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v5
        with:
          name: playwright-audio-report
          path: playwright-report/
          retention-days: 14
